# webmcp

This skill describes browser-level WebMCP context exposure.

- Implemented in `assets/js/webmcp.js`
- Uses `navigator.modelContext.provideContext()` when available
- Exposes tools for quote flow navigation and insurance page listing
